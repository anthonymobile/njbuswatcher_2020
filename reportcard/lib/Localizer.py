import scipy.spatial as sp

def infer_stops(bus_data, stopslist): # called from routewatcher.py
    # takes a bus_data object
    # and a stopslist from lib.BusAPI.parse_xml_getRoutePoints

    sp.whatever

    # log the results
    bus_data.stop_id =
    bus_data.closest_approach_distance
    bus_data.duration_in_proximity_to_stop

    # and return

    return bus_data